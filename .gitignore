# Root .gitignore for MERN stack

# Node modules (both client and server)
client/node_modules/
server/node_modules/

# Build and production files
client/build/
server/dist/

# Logs
client/logs
server/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS system files
.DS_Store
Thumbs.db

# Environment variables
client/.env
server/.env
.env

# IDEs and editors
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# npm/yarn/lock files (optional to ignore one or the other)
# Uncomment only if you're not committing lockfiles
# client/package-lock.json
# server/package-lock.json
# client/yarn.lock
# server/yarn.lock

# Coverage and test output
coverage/
client/coverage/
server/coverage/
*.lcov

# Optional: Cache files
client/.cache/
server/.cache/
